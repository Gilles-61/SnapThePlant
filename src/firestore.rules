
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access to species and their images
    match /species/{speciesId} {
      allow read, write, delete: if true;
    }
    match /imageCache/{imageId} {
      allow read, write, delete: if true;
    }
  }
}
